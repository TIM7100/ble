# 更新内容（历史总体）

当前汇总记录本项目（HP9_90X 下位机固件 + 相关 Boot 工程 + test3 手机 App）历史上所有更新内容。

## 一、固件 OTA 升级（task9）实现
- 使用 test3 的"固件 OTA 升级"按钮控制 task9，用于替换旧的上位机升级工具。
- 流程：云上下载 IAP 固件到本地 → 发送到下位机 → 进入 OTA 进行固件升级。
- 实现 OTA bootloader 协议（START→分区信息→数据块→OTA完成→REBOOT），支持 hex16 固件解析、XIP + SRAM 分区正确烧录。
- 修正 OTA 进入/退出后的重连逻辑（bootloader 广播地址 = 原 MAC+1，优先连 +1 地址，避免等待 30s）。
- 优化 bootloader OTA 对齐，保证升级完成后能正常启动。

## 二、固件本地版本对比 + 云端版本（task9 升级前判断）
- 下位机新增 `OTAfile_handle.c/.h`（components/OTAEVENT）：当前版本存放于 0x11041000，待定版本存放于 0x11042000。
- 新增 OTA 命令：`'4'` 对比、`'5'` 写入待定版本、`'6'` 返回本地版本。
- 点击"固件 OTA 升级"先对比本地与云端版本：一致则提示"已是最新，无需升级"，不同才下载升级。
- 升级成功后、重启前由 bootloader 将待定版本提升为当前版本，避免 OTA 失败误改本地版本。

## 三、Boot 工程（OTA_internal_flash / 共享 SDK ota_protocol.c）
- 在 OTA 全部写入成功、重启前，把"待定固件版本"(0x11042000)提升为"当前版本"(0x11041000)。

## 四、test3 App 界面
- 主页在 HP9 数据版本下方新增"固件本地版本号 / 固件云端版本号"显示，并做中英文切换。
- "本地HP9数据版本号 / 云端HP9数据版本号"等文案中文化、双语。
- "HP9 数据升级 / 固件 OTA 升级"两个按钮改为中英文，并调整间距、内边距、等宽。
- OTA 升级进度时的提示文字按蓝牙/I2C 区分（蓝牙："请勿断开蓝牙连接"；I2C 保持"请勿断开与芯片连接"）。

## 五、其它/历史
- 设备名显示问题排查（DEVICE_NAME_FLASH_ADDR / MX--90X，未改动，仅分析）。
- 修复了固件版本对比读取到旧响应导致的误判（独立标志位）。
- 移除"触发 OTA 前直接回写当前版本"的旧逻辑，改为写入待定槽，避免失败误判。

## 六、下载文件自动清理（防止手机内存堆积）
- 新增 `clearDownloadFiles()`：在**固件 OTA 升级完且 HP9 数据升级完成**后，清理云上下载目录（PUBLIC_DOWNLOADS）中的文件，防止堆积。全流程中仅在一处调用（HP9 数据实际升级完成的收尾点）。
- 修复清理误删正在复用的 HP9 数据 .bin：当本地版本 == 云端版本时会复用已下载的 .bin，清理时跳过该当前数据文件（取自 `globalData.path`），只删除其它临时下载文件（固件 IAP、旧时段残留、APP 等），避免下一轮连接时读取不到文件而卡住。

## 七、APP 版本升级（设置页）修复
- 修复"下载完成后找不到安装包"：原逻辑在 `plus.runtime.install` 的 success 回调（仅代表系统已接收安装意图，尚未读完 APK）立即删除 APK，造成系统安装器找不到安装包。
- 改为安装时**不立即删除** APK，让其留在下载目录，由 index 页完整升级完成后的 `clearDownloadFiles()` 统一清理。

## 八、OTA 升级方案演进（SLB 冲突 → 回归 Single Bank OTA）
- 两种 OTA 升级方式（SLB vs 原工程 Single Bank OTA）存在冲突，无法同时进行。
- 首次尝试使用 SLB 模式，但使用官方提供的 SLB HEX 源文件 + 官方 APP 也无法完成升级。
- 结论：放弃 SLB，回归使用原有的 Single Bank OTA 升级模式进行修改。

## 九、任务整合与 APP 通信兼容（OSAL 框架）
- 使用例程中相同升级模式的工程 + 官方手机 APP 可进行固件升级，但与蓝牙升级工程有冲突，无法直接进行多项升级，且与蓝牙工程 APP 端无法通信。
- 工程框架为 OSAL：增加原有升级模式的任务数量，尝试把两个工程任务进行兼容。
- 初步整合后可使用对应 bootloader 进行程序跳转，可分别与两个不同手机 APP 连接升级，但无法进行单个手机 APP 升级。
- 通过修改大部分冲突地址、修改信号连接的初始化内容、减少两个任务内的重复初始化、修改两者 UUID 与手机 APP 生成的 ID 进行对接 → 实现单个 APP 通信。

## 十、云端固件上传与 HEX16 解析
- 上传云端 bin 文件进行烧录：修改增加云端数据集，增加手机 App 下载对应固件功能。
- 蓝牙升级仪接收对应文件并解析，但下载后无法升级。
- 修改为官方上位机工具生成的 HEX16 文件后，可正确读取分区信息进行升级。

## 十一、手机 App 升级逻辑优化
- 改为一连接就自动进行固件检测 + 原本的数据检测，从云端下载到本地。
- 增加固件升级的版本比对功能，避免重复下载；按型号比对下载对应型号的数据。
- 连接后自动升级固件版本和 HP9 数据，用户无需手动操作（简化手机 App 显示）。
- 中途协助修改了 flash 切换。

## 十二、升级稳定性修复
- 修复 bug：连接超时无法比对数据时会直接进行固件更新（无论是否最新固件）。
- 增加超时重连机制，提高升级稳定性（详见第一节 OTA 重连逻辑与第二节超时处理）。

## 十三、本地下载文件堆积与误发旧数据
- 问题：本地手机下载文件过多不自清理，导致堆积，发送时可能发送到非最新数据包。
- 解决：新增下载文件自动清理 `clearDownloadFiles()`（详见第六节）。

## 说明
- 以上更新需分别重新编译：下位机固件、Boot 工程、test3 App 后生效。
- 云端 IAP 集合的 `version` 字段 = "固件云端版本号"；hex16 固件下载到手机 Download 目录。